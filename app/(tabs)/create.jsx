import { View, Text } from 'react-native'
import React from 'react'
import { CreateProfile } from '../../components/helpers'

const Create = () => {
  return (
    <CreateProfile create={true}/>
  )
}

export default Create